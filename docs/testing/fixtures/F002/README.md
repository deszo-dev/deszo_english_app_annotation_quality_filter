# F002 full traversal fixture

This fixture covers all 10 official traversal paths defined by `annotation_quality_filter` v2.0. It is intentionally small, deterministic, and uses path-specific `ref.kind` values for each text unit.

Use it for TC-002 / TC-P1-025.
